target-version = "py312"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# This isolated test entrypoint imports only host files extracted from the npm artifact.
"tests/integration/jobs/application.py" = ["E402"]
